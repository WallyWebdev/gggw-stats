{
  "name": "gggw-stats",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro check && astro build",
    "check": "astro check",
    "preview": "astro preview",
    "test": "playwright test",
    "validate": "python3 scripts/validate_data.py --live && npm run build && npm test",
    "scrape": "python3 scripts/scrape_walks.py"
  },
  "dependencies": {
    "@astrojs/check": "0.9.10",
    "astro": "7.1.6",
    "leaflet": "1.9.4",
    "typescript": "6.0.3"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1",
    "@types/leaflet": "1.9.20"
  }
}
